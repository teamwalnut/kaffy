mutation MoveScreens(
  $screenIds: [ID!]!
  $targetFlowId: ID!
  $newPosition: Int!
) {
  moveScreens(
    screenIds: $screenIds
    targetFlowId: $targetFlowId
    newPosition: $newPosition
  ) {
    id
    name
  }
}
